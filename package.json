{
  "name": "wp-cli-shifter",
  "scripts": {
    "setup": "bash bin/install-wp-tests.sh wordpress_test root '' localhost latest && WP_CLI_BIN_DIR=/tmp/wp-cli-phar bash bin/install-package-tests.sh",
    "test": "phpunit && WP_CLI_BIN_DIR=/tmp/wp-cli-phar ./vendor/bin/behat"
  },
  "license": "GPLv2"
}
